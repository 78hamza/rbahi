so now i add the mongodb data connection  to the backend connection successfully, now listen to this and return what do you think is possibel structured or there is a better plan. from fronttend we will send a request to the backend containe the userID, dataset, and created_at (mongose schema like this btw), then in the backend first thing is to store this request schema into our database in mongo, then we send a request to the microservice containe thsi dataset andits userId, then the microservice enter this dataset into the recommendation system engine and return the results in json format and
